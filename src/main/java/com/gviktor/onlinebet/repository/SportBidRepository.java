package com.gviktor.onlinebet.repository;

import com.gviktor.onlinebet.model.SportBid;
import org.springframework.data.jpa.repository.JpaRepository;

public interface SportBidRepository extends JpaRepository<SportBid,Integer> {
}
