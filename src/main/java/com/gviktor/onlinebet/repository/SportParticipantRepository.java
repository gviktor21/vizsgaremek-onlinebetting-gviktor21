package com.gviktor.onlinebet.repository;

import com.gviktor.onlinebet.model.SportParticipant;
import org.springframework.data.jpa.repository.JpaRepository;

public interface SportParticipantRepository extends JpaRepository<SportParticipant,Integer> {
}
